When i click the donut, there is a blakc outline and when i hover over a section, theres an outline to it.
Also, when loading the page, if i interrupt it while its still spinning, it stops spinning And then starts back up. It should never be able to be
interrupted.

When I click a section of the pie chart, the tooltip permanently stays above the piechart now even if i move my mouse away.

I also get this warning in the console: is this related to anything we've done and can we fix it?
[browser] The width(-1) and height(-1) of chart should be greater than 0,
       please check the style of container, or the props width(100%) and height(100%),
       or add a minWidth(0) or minHeight(undefined) or use aspect(undefined) to control the
       height and width. (<anonymous>)
[browser] The width(-1) and height(-1) of chart should be greater than 0,
       please check the style of container, or the props width(100%) and height(100%),
       or add a minWidth(0) or minHeight(undefined) or use aspect(undefined) to control the
       height and width. (<anonymous>)

The pie chart sections aren't hovering on hover anymore like they used to (or animating) after the refactor, fix that


Fix those issues, and from now on, when the rate limit is at 10% for for codex, generate a summary so I can pass this onto another
AI which will be able to easily continue off with enough context.

One more thing, you should avoid long inline logic inside jsx props.
Preferred:
const computedValue = doSomething();

<select prop={computedValue} />

or 

<select prop={doSomething()} />

Based on this, make the necessary adjustments to the codebase.